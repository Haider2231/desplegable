{
  "name": "fullstack-app",
  "scripts": {
    "build": "cd gestion_futbol/frontend && npm install && npm run build",
    "start": "cd gestion_futbol/backend && npm install && node index.js"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^4.4.9"
  }
}
